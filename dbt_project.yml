name: "instacart"
version: '1.11.11'
profile: "instacart"

model-paths: ["dbt/models"]

models:
  instacart:
    staging:
      +materialized: view
      +schema: STAGING
    
    intermediate:
      +materialized: view
      +schema: INTERMEDIATE
    
    marts:
      +materialized: table
      +schema: MARTS